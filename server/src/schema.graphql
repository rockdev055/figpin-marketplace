# import * from './generated/prisma.graphql'

type Mutation {
  createPin(
    name: String
    figpinID: Int
    exclusive: Boolean
    image: String
    largeImage: String
    volumnSize: Int
    edition: String
    price: Int
  ): Pin!
}

type Query {
  getPins: [Pin]!
}
